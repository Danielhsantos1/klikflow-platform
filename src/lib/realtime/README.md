# Realtime

Placeholder for Supabase Realtime channel helpers (order/tab status updates,
production board updates, etc.). No channels are wired yet — this directory
exists so that feature code has one place to import realtime subscriptions
from once the schema they observe is defined.
